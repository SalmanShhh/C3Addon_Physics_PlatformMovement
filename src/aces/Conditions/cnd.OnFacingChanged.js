export const config = {
  isTrigger: true,
  listName: "On facing changed",
  displayText: "On facing changed",
  description: "Triggered when the character turns around.",
  params: [],
};

export const expose = true;

export default function () {
  return true;
}
