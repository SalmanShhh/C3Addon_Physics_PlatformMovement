export const config = {
  isTrigger: true,
  listName: "On fallen off",
  displayText: "On fallen off",
  description: "Triggered when the character walks off a ledge.",
  params: [],
};

export const expose = true;

export default function () {
  return true;
}
