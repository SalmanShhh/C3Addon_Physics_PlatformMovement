export const config = {
  listName: "On hit ceiling",
  displayText: "On hit ceiling",
  description: "Triggered when the character first makes contact with a ceiling.",
  isTrigger: true,
  params: [],
};

export const expose = false;

export default function () {
  return true;
}
